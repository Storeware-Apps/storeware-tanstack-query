import React, { ReactNode } from "react";
interface QueryProviderProps {
    children: ReactNode;
}
export declare const QueryProvider: ({ children }: QueryProviderProps) => React.JSX.Element;
export {};
